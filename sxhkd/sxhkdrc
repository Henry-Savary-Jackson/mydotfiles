
alt + shift + Return 
  kitty

alt + shift + h
  kitty htop

alt + r
  kitty ranger

alt + shift + f
  firefox

alt + shift + l
  lock

alt + shift + n
  kitty nvim

alt + shift + t
  torbrowser-launcher

alt + shift + v
  codium

alt + shift + m
  kitty ncmpcpp

XF86KbdBrightnessUp
  light -s sysfs/leds/:white:kbd_backlight -A 20

XF86KbdBrightnessDown
  light -s sysfs/leds/:white:kbd_backlight -U 20

XF86MonBrightnessUp
  brillo -A 10

XF86MonBrightnessDown
  brillo -U 10

XF86AudioMute
  amixer set Master 1+ toggle

XF86AudioRaiseVolume
  amixer set Master 10%+

XF86AudioLowerVolume
  amixer set Master 10%-

super + m + {p,s,less, greater,b, f}
  mpc { toggle, stop, prev, next, seek -10%, seek +10%,}
